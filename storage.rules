rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Standard rule: allow all read/write for authenticated users
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
